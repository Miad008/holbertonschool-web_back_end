#!/usr/bin/env python3

"""
Module that contains an add function which takes two float arguments and returns their sum as a float.
"""
def add(a: float, b: float) -> float:
    """Adds two float numbers and returns the sum as a float."""
    return a + b
